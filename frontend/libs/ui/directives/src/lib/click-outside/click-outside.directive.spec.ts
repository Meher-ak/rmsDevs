import { ClickOutsideDirective } from './click-outside.directive';

describe('ToggleDropdownMenuDirective', () => {
  it('should create an instance', () => {
    // const directive = new ClickOutsideDirective();
    // expect(directive).toBeTruthy();
  });
});
